<?xml version="1.0" encoding="utf-8"?>
<!--
    Root layout: a custom GameView fills the screen; a thin overlay on top
    shows the current score and the all-time best score.
    Phase 3 will add full-screen overlays (menu / pause / game over) here.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:keepScreenOn="true">

    <com.snakerush.ui.GameView
        android:id="@+id/gameView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:id="@+id/scoreBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/scoreText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/text_primary"
            android:textSize="20sp"
            android:textStyle="bold"
            android:text="@string/score_label" />

        <TextView
            android:id="@+id/bestScoreText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_secondary"
            android:textSize="16sp"
            android:text="@string/best_label" />
    </LinearLayout>

    <!--
        On-screen D-pad (second input method, alongside swipe). Both route to
        GameView.pressDirection(...). The GameView reserves board_bottom_reserve
        so the board sits above these buttons.
    -->
    <LinearLayout
        android:id="@+id/dpad"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:orientation="vertical"
        android:paddingBottom="16dp">

        <Button
            android:id="@+id/btnUp"
            android:layout_width="56dp"
            android:layout_height="48dp"
            android:layout_gravity="center_horizontal"
            android:background="@drawable/dp_button"
            android:padding="0dp"
            android:text="▲"
            android:textColor="@color/text_primary"
            android:textSize="18sp" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Button
                android:id="@+id/btnLeft"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/dp_button"
                android:padding="0dp"
                android:text="◀"
                android:textColor="@color/text_primary"
                android:textSize="18sp" />

            <Button
                android:id="@+id/btnDown"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/dp_button"
                android:padding="0dp"
                android:text="▼"
                android:textColor="@color/text_primary"
                android:textSize="18sp" />

            <Button
                android:id="@+id/btnRight"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/dp_button"
                android:padding="0dp"
                android:text="▶"
                android:textColor="@color/text_primary"
                android:textSize="18sp" />
        </LinearLayout>
    </LinearLayout>

</FrameLayout>
